<h2>User info</h2>
  <%= image_tag user.get_profile_image(100,100) %>
  <table class="table">
    <tbody>
      <tr></tr>
      <tr>
        <th>name</th>
        <th><%= user.name %></th>
      </tr>
      <tr>
        <th>introduction</th>
        <th><%= user.body %></th>
      </tr>
    </tbody>
  </table>
  <div class="row">
    <%= button_to edit_user_path(user.id), class: "btn btn-outline-secondary btn-block", method: :get do %>
      <i class="fas fa-user-cog"></i>
    <% end %>
  </div>